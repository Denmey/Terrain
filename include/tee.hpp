#pragma once

#include <glm/glm.hpp>
#include <iostream>

// template <typename T>
// decltype(auto) tee(T&& value, const std::string& message = "")
// {
// 	std::cout << message << value << "\n";
// 	return std::forward<T>(value);
// }

// decltype(auto) tee(glm::vec3&& value, const std::string& message = "") {
// 	std::cout << message << value.x << " " << value.y << " " << value.z << "\n";
// 	return value;
// }

// #define TEE(x) tee( (x), #x " " )
